language: "node_js"
node_js:
  - "node"
install:
  - "npm install -g gitbook"
  - "npm install -g gitbook-cli --force"
branches:
  only:
    - master
env:
  global:
    - GH_REF: github.com/bond-huang/huang.git
script:
  - bash summary_crt.sh
  - travis_wait 100 bash deploy.sh
